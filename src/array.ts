let nums: number[] = [1, 2, 3, 4, 5]; // 숫자만 허용
let strs: string[] = ["a", "b", "c"]; // 문자열만 허용
let bools: boolean[] = [false, false, true];

// 요소 추가
// nums.push(6);
// nums.push(7);

// nums.push("8");

// nums[0] = "4";

bools.push(1);

// let nums: Array<number> = [1, 2, 3, 4, 5]; // 숫자만 허용
// let strs: Array<string> = ["a", "b", "c"]; // 문자열만 허용

// // 요소 추가
// nums.push(6);
// nums.push(7);

// nums.push("8");

// nums[0] = "4";
